# Документация расширения VS Code: Snippet Commands

## Описание

Данное расширение для Visual Studio Code добавляет пользовательские команды для быстрой вставки шаблонов кода (snippets) популярных конструкций языка JavaScript:

- if
- for
- while

Расширение предназначено для ускорения написания кода за счёт автоматической вставки базовых заготовок управляющих конструкций в позицию курсора.

---

## Основная логика работы

1. При активации расширения вызывается функция activate.
2. Внутри activate регистрируются три команды VS Code:
   - snippet.if
   - snippet.for
   - snippet.while
3. Каждая команда при вызове вставляет соответствующий текстовый шаблон в активный редактор.
4. Вставка текста реализована через вспомогательную функцию insertSnippet.
5. Все зарегистрированные команды добавляются в context.subscriptions для корректного управления жизненным циклом расширения.
6. При деактивации расширения вызывается функция deactivate (в текущей реализации она пуста, но обязательна по API).

---

## Структура кода

### Импорт зависимостей

```js
const vscode = require('vscode');
```
Импортируется API Visual Studio Code, необходимое для:
регистрации команд,
работы с редактором,
отображения сообщений об ошибках.

Функции
activate(context)
Описание:
Основная функция активации расширения. Вызывается автоматически при активации расширения в VS Code.
Параметры:
context (vscode.ExtensionContext) — контекст расширения, используется для управления подписками и ресурсами.
Назначение:
Регистрирует все команды расширения.
Добавляет их в context.subscriptions.
Регистрируемые команды:
snippet.if
Вставляет шаблон условного оператора:
```
if (condition) {
    
}
```
snippet.for
Вставляет шаблон цикла for:
```
for (let i = 0; i < n; i++) {
    
}
```
snippet.while
Вставляет шаблон цикла while:
```
while (condition) {
    
}
```
Пример вызова:

activate(context);

insertSnippet(text)
Описание:
Вставляет переданный текстовый шаблон в активный текстовый редактор в текущую позицию курсора.
Параметры:
text (string) — текст шаблона для вставки.
Возвращаемое значение:
void
Логика работы:
Получает активный текстовый редактор.
Если редактор отсутствует — показывает сообщение об ошибке.
Если редактор есть — вставляет текст в позицию editor.selection.start.
Пример вызова:

insertSnippet("if (condition) {\n\t\n}");
Обработка ошибок:
Если активный редактор отсутствует, выводится сообщение:

Нет активного редактора

deactivate()
Описание:
Функция деактивации расширения.
Назначение:
Вызывается при деактивации расширения в VS Code.
В текущей реализации не выполняет никаких действий, но требуется API расширений.
Возвращаемое значение:
void

Экспортируемые элементы
```
module.exports = {
    activate,
    deactivate
};
```
Экспортируются функции activate и deactivate, которые используются VS Code для управления жизненным циклом расширения.

История коммитов
cc4d45e — Реализация плагина
8bcadaa — Текущая версия (HEAD → main)

Итог
Данное расширение реализует простой, но полезный механизм пользовательских команд для вставки шаблонов кода. Архитектура соответствует рекомендациям VS Code API и легко расширяется добавлением новых snippet-команд.
